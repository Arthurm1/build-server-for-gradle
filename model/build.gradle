plugins {
    id 'java-library'
}

java {
  toolchain {
    languageVersion = JavaLanguageVersion.of(8)
  }
}

tasks.withType(JavaCompile) {
  options.compilerArgs += ['-Xlint']
}

dependencies {
    api 'org.gradle:gradle-tooling-api:8.12'
}